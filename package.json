{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.1.0"
  },
  "scripts": {
    "graph": "npx nx graph",
    "create": "lerna create",
    "test": "lerna run test",
    "f:version": "lerna version",
    "f:publish": "lerna publish --no-private",
    "build": "lerna run build"
  }
}
